div id='hero-container'
  ul class='cb-slideshow'
    - @banners.each do |banner|
      li
        span style="background-image: url(#{banner.image_url})"

  = render partial: 'misc/home_header'
  = render partial: 'misc/home_caption'
  /= render partial: 'misc/search_panel'
